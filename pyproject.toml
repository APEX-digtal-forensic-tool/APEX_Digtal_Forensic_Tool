[build-system]
requires = ["setuptools>=78.1.1,<81"]
build-backend = "setuptools.build_meta"

[project]
name = "apex-forensic"
version = "0.1.0"
description = "APEX Phase 1 forensic core foundation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "defusedxml>=0.7.1",
  "jsonschema>=4.22",
  "rfc3339-validator>=0.1.4",
  "pytsk3>=20260715",
  "tzdata; platform_system == 'Windows'",
]

[project.optional-dependencies]
dev = [
  "bandit>=1.9,<2",
  "mcp==2.1.1",
  "mypy>=1.10",
  "pytest>=8.2",
  "ruff>=0.5",
  "types-defusedxml>=0.7.0",
]
mcp-server = [
  "mcp==2.1.1",
  "uvicorn==0.52.4",
]
windows-artifacts = [
  "python-registry>=1.3.1",
  "python-evtx>=0.8.1",
  "regipy>=6.3.0",
  "dissect.util>=3.24",
  "pywin32>=306; platform_system == 'Windows'",
]
browser-media = [
  "Pillow>=10.0",
  "cryptography>=42.0",
]
advanced-secrets = [
  "impacket>=0.13,<0.14",
]
machine-extraction = [
  "rapidocr>=3,<4",
  "onnxruntime>=1.18,<2",
  "faster-whisper>=1.2,<2",
]
report-renderer = [
  "reportlab>=4.2",
]
filesystem-image = [
  "pytsk3>=20260715",
]
expert-witness = [
  "libewf-python>=20240506",
]
virtual-disk = [
  "libvhdi-python>=20251119",
]

[project.scripts]
apex-forensic = "apex_forensic.cli:main"
apex-mcp = "apex_mcp.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
apex_forensic = ["py.typed"]
apex_mcp = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "C4", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
pretty = true
files = ["src/apex_forensic", "src/apex_mcp"]
mypy_path = "src"
explicit_package_bases = true

[[tool.mypy.overrides]]
module = ["jsonschema.*"]
ignore_missing_imports = true
