# APEX FTK 레이아웃 Design QA

final result: passed

## 비교 대상과 상태

- Source visual truth: 공식 FTK Imager 8.2 User Guide, PDF 53쪽에 수록된 실제 앱 화면. 추출 파일 `/private/tmp/apex-ftk-viewer-reference.png` (1307×680). PDF 28쪽 화면 `/private/tmp/apex-ftk-official-reference.png` (1101×626) 및 인쇄 16–24쪽의 패널 설명도 함께 확인.
- [공식 원문](https://d1kpmuwb7gvu1i.cloudfront.net/Imager/8.2/Exterro%20FTK%20Imager%208.2%20-%20User%20Guide%20.pdf).
- Implementation: `docs/desktop/ftk-redesign/screens/08-workbench-final.png`, 1333×986 px. CSS viewport 1333×986, DPR 1, 합성 데모 사건의 README.txt 선택 / Hex 표시 / 기본 패널 배치.
- Compact: `docs/desktop/ftk-redesign/screens/07-compact-final.png`, 1024×768 px. CSS viewport 1024×768, DPR 1, 두 번째 증거 선택.
- Source와 implementation 이미지를 하나의 비교 입력으로 열어 전체 구성을 확인했다. 이미지 밀도 보정 없이 원래 크기로 보았다. 목적은 FTK의 도킹 패널 구조를 적용하는 것이며 픽셀 단위 테마 복제가 아니다.
- 상태 차이: 공식 이미지에는 밝은 Windows 테마, 빈 증거 목록, 복호화 대화상자가 있다. APEX에는 사용자가 요청한 다크 테마, 합성 데이터, APEX 로고와 지정 아이콘을 사용한다. 해당 차이를 불일치로 판정하지 않았다. 실제 파일 내용의 시각적 일치는 주장하지 않는다.

## 확인 및 수정 이력

1. 기존 화면: `01-before-overview.png`, `02-before-files.png`. 전체 증거 트리 부재, 두 겹의 세로 탐색, 속성과 내용의 혼재를 확인했다.
2. 초기 구현: `03-layout-draft.png`, `04-hex-draft.png`. 네 패널, 상단 도구 모음, 가로 분석 탭, Hex 주소/바이트/ASCII 열을 확인했다. 색과 패널 위치는 의도한 방향이었다.
3. [P2, 수정 완료] 작은 창에서 활성 증거가 트리 스크롤 아래로 가려짐. `06-compact.png`에서 현재 증거는 외장 USB인데 트리의 해당 행이 보이지 않았다. 활성 폴더를 펼치고 `scrollIntoView`로 표시하도록 수정했다. `07-compact-final.png`에서 외장 USB와 활성 루트가 동시에 보인다.
4. [P2, 수정 완료] 데모 상태를 실제 엔진 연결처럼 표시하던 상태 표시줄을 구분했다. 최종 캡처 하단에는 `데모 · 합성 데이터`가 표시된다.
5. 최종 비교: `08-workbench-final.png`에서 증거 트리/속성과 파일 목록/내용 뷰어가 좌우로 유지되고 두 하단 패널의 경계가 정렬된다. README 선택 표시와 속성, Hex 헤더와 바이트 열이 일치한다. 남은 P0/P1/P2 시각적 문제는 발견하지 않았다.

## 필수 표면 검토

- Typography: 운영체제 sans-serif와 고정폭 Hex 글꼴을 사용한다. 테이블·탭 11px, 원본 텍스트 12px로 밀도를 높였다. 한글 파일명과 긴 ID는 속성에서 줄바꿈하고 목록에서는 잘라 표시한다. 원본 Hex 열은 일정한 문자 폭으로 정렬한다.
- Spacing/layout: 42px 상단, 36px 도구 모음, 280px 기본 트리, 30px 목록 행, 340px 기본 하단 패널. 작은 창에서는 테이블/탭 내부 스크롤을 사용하며 전체 문서 가로 너비는 1024px를 넘지 않음을 DOM으로 확인했다. 패널 키보드 조절과 초기화를 확인했다.
- Colors/tokens: 어두운 중립 표면과 차분한 청회색 선택 표시. FTK의 밝은 테마와 다른 것은 사용자 요구에 따른 의도된 변경이다. 장식용 카드/그라데이션은 추가하지 않았다.
- Assets: 제공된 APEX 로고, 기존 Astryx 래퍼, Material 파일 아이콘을 재사용했다. 임의로 그린 로고나 FTK 아이콘 대체 이미지는 없다. 최종 원본 크기 캡처에서 자산 선명도와 파일 아이콘 구분을 확인했다.
- Copy/content: 증거 트리, 파일 목록, 속성, 내용 뷰어로 역할을 구분한다. 원본 읽기 전용과 합성 데이터 상태를 명시한다. 미지원 바이트 읽기와 빈 폴더는 분석 실패와 구분한다. 기존 인용·경고·출처·보고서 근거 수집을 유지한다.
- Focused comparison: 원본 크기 전체 캡처에서 각 패널의 표제, 목록, 속성, Hex 열이 읽힐 만큼 선명해 추가 확대 캡처는 필요하지 않았다. 공식 자료는 빈 화면/모달 상태여서 개별 파일 내용의 동일성 비교 대상이 아니다.

## 동작 확인과 한계

브라우저에서 사건/증거 변경, 폴더 더블클릭/상위 이동, 경로·트리 동기화, Preview/Text/Hex/메타데이터, `0x10` 오프셋, 가로 구분선의 키보드 이동, 배치 초기화, 아티팩트 화면에서 트리 탐색 복귀를 확인했다. 1024×768 확인 후 viewport override는 초기화했다.

콘솔을 확인했다. 편집 중 파일 생성/HMR과 hook dependency 수정으로 발생한 이전 경고가 기록에 남아 있었지만, 최종 전체 새로고침과 탐색 후 새 오류는 관찰되지 않았다. 최종 캡처 시각은 2026-09-16 09:46 UTC 부근이며 남아 있는 경고의 마지막 시각은 09:38 UTC였다.

TypeScript 검사와 Vite 빌드 통과. 기존 회귀 테스트의 이전 메뉴/검사기 선택자는 새 UI와 고정 4 KiB 읽기에 맞춰 갱신했으며, 자동 테스트 스위트는 실행하지 않았다. 실제 증거 파일, 이미지 미리보기, 네이티브 배포 패키지 및 Windows/macOS 전체 회귀 검증은 미실행이다. 이 판정은 레이아웃과 브라우저 데모 탐색에 대한 것으로 Core 엔진 검증 완료를 뜻하지 않는다.

P3 후속: 대규모 번들에 대한 코드 분할 검토. 현재 빌드는 성공하지만 500 kB 경고가 있다.

## 후속 수정

- 일반 파일을 증거 트리에 포함하고 선택 시 부모 폴더 목록·속성·내용 뷰어가 함께 전환되도록 연결했다. TypeScript 검사와 브라우저 데모 탐색으로 확인했다.
- 제공된 정사각형 로고의 실제 도형 경계에 맞춰 비율 기반 표시 영역을 적용했다. 원본 이미지를 변경하지 않고 오른쪽 X가 잘리던 현상을 수정했으며 브라우저에서 전체 로고 표시를 확인했다.
- 위 자동 테스트 미실행 표기는 로컬 디자인 작업 단계에 대한 기록이다. 배포 검증 결과는 해당 GitHub 릴리즈의 CI 링크와 출처 파일을 기준으로 확인한다.
